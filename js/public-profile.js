document.addEventListener('DOMContentLoaded', async () => {
  const root=document.getElementById('profile-root'); const params=new URLSearchParams(location.search); const viewedId=params.get('id')||params.get('profile_id')||params.get('user_id');
  if(!viewedId){root.innerHTML='<div class="card"><h3>Profile not found</h3><p class="muted">No profile was selected.</p></div>';return;}
  const failTimer=setTimeout(()=>{if(root.querySelector('.profile-loading'))root.innerHTML='<div class="card"><h3>Couldn’t load this profile</h3><p class="muted">Please refresh once. If it still fails, the profile may be private or unavailable.</p><a class="btn btn-primary" href="dashboard.html">Back to matches</a></div>';},10000);root.innerHTML='<div class="card profile-loading"><p>Loading profile…</p></div>';
  try{if(!window.supabaseClient)throw new Error('Supabase client is not initialized.');const{data:{user},error:authError}=await supabaseClient.auth.getUser();if(authError)throw authError;if(!user){location.href='login.html';return}if(user.id===viewedId){location.href='profile.html';return}
    const[{data:profile,error},{data:myRequests},{data:acceptedChat}]=await Promise.all([supabaseClient.from('profiles').select('*').eq('id',viewedId).maybeSingle(),supabaseClient.from('roommate_merge_requests').select('id,status,requester_id,recipient_id').or(`and(requester_id.eq.${user.id},recipient_id.eq.${viewedId}),and(requester_id.eq.${viewedId},recipient_id.eq.${user.id})`).order('created_at',{ascending:false}).limit(5),supabaseClient.from('conversations').select('id,status').eq('status','active').or(`and(participant_a.eq.${user.id},participant_b.eq.${viewedId}),and(participant_a.eq.${viewedId},participant_b.eq.${user.id})`).maybeSingle()]);if(error)throw error;if(!profile)throw new Error('This profile is not visible or no longer exists.');
    const initials=(profile.full_name||'?').split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase(),avatar=profile.photo_url?`background-image:url('${String(profile.photo_url).replace(/'/g,'%27')}')`:'';const premium=profile.is_premium||profile.premium_since;const lifestyle=[['🌙','Sleep schedule',profile.sleep_schedule],['🧹','Cleanliness',profile.cleanliness],['👥','Guests',profile.guest_frequency],['🧠','Personality',profile.personality],['🚭','Smoking / drinking',profile.smoking_drinking],['🍳','Cooking',profile.cooking_habits],['🗣️','Conflict style',profile.conflict_style]].filter(x=>x[2]!==null&&x[2]!==undefined&&String(x[2]).trim()!=='');const verification=[['Platform identity',profile.identity_verified],['Phone',profile.phone_verified],['Email',profile.email_verified],['Room listing',profile.room_verified]],verified=verification.filter(x=>x[1]===true).length,joinDate=profile.created_at?new Date(profile.created_at).toLocaleDateString('en-IN',{month:'short',year:'numeric'}):'Recently';const existing=myRequests?.[0],isAccepted=!!acceptedChat||existing?.status==='accepted';const action=isAccepted?`<a class="btn btn-primary" href="chat.html">💬 Open private chat</a>`:`<button class="btn btn-primary" id="merge-btn">🤝 Raise merge request</button>`;
    root.innerHTML=`<section class="card profile-public-hero"><div class="profile-hero"><div class="public-avatar" style="${avatar}">${profile.photo_url?'':initials}</div><div><span class="eyebrow">Roommate profile</span><h1 style="margin:4px 0 0">${esc(profile.full_name||'Unnamed')}</h1><p class="muted" style="margin:6px 0">${esc([profile.age?profile.age+' years':'',profile.gender||'',profile.city||'',profile.preferred_area||''].filter(Boolean).join(' · '))}</p><div class="profile-badges"><span class="pill">🏠 Looking for a roommate</span>${premium?'<span class="pill">⭐ Premium</span>':''}${profile.is_verified===true?'<span class="pill">✓ Platform verified</span>':''}<span class="pill">📅 On HomeSync since ${joinDate}</span></div></div><div class="profile-action">${action}<a class="btn btn-ghost" href="trust-center.html">🛡️ Trust Center</a></div></div><div class="privacy">🔒 <strong>Privacy protected.</strong> Exact address, live location, phone number and private questionnaire answers are never shown publicly. Location sharing becomes available only inside an accepted private match.</div></section><div class="profile-grid"><div class="card info-card"><h4>👋 About</h4><p style="white-space:pre-wrap">${esc(profile.bio||'This person has not added a bio yet.')}</p></div><div class="card info-card"><h4>💰 Budget & move-in</h4><p><strong>₹${profile.budget_min||'—'} – ₹${profile.budget_max||'—'}</strong> / month</p><p class="muted">Move-in: ${esc(profile.move_in_date||profile.available_from||'Flexible')}</p></div><div class="card info-card"><h4>📍 Neighbourhood</h4><p><strong>${esc(profile.preferred_area||profile.city||'Not specified')}</strong></p><p class="muted">Exact coordinates stay private. After a merge is accepted, you can create a protected visit and share live location by consent.</p></div><div class="card info-card"><h4>🛡️ Trust signals</h4><div class="verify-count">${verified}/${verification.length} listed checks</div><div class="profile-badges">${verification.map(x=>`<span class="pill">${x[1]===true?'✓':'○'} ${x[0]}</span>`).join('')}</div><p class="muted" style="margin-top:10px">A missing badge is not proof of wrongdoing. It simply means HomeSync has not verified that signal.</p></div><div class="card info-card" style="grid-column:1/-1"><h4>✨ Lifestyle</h4><p class="muted">Public compatibility signals — private questionnaire answers stay hidden.</p><div class="profile-lifestyle-grid">${lifestyle.length?lifestyle.map(x=>`<div class="lifestyle-item"><span>${x[0]}</span><div><small>${esc(x[1])}</small><strong>${esc(formatValue(x[2]))}</strong></div></div>`).join(''):'<p class="muted">Lifestyle details not added yet.</p>'}</div></div><div class="card info-card" style="grid-column:1/-1"><h4>🤝 Match safety path</h4><div style="display:grid;grid-template-columns:repeat(4,1fr);gap:10px"><div class="pill">1. Compare vibe</div><div class="pill">2. Merge request</div><div class="pill">3. Private chat</div><div class="pill">4. Safe visit</div></div><p class="muted" style="margin-top:12px">HomeSync keeps the stages separate so a stranger does not immediately receive your phone number, exact address or live location.</p></div><div class="card info-card" style="grid-column:1/-1"><h4>📝 Posts & updates</h4><div id="profile-posts"><p class="muted">Loading posts…</p></div></div></div>`;
    document.getElementById('merge-btn')?.addEventListener('click',async()=>{const btn=document.getElementById('merge-btn');btn.disabled=true;btn.textContent='Checking profile readiness…';try{const{error}=await supabaseClient.rpc('create_roommate_merge_request',{p_target_user_id:viewedId});if(error)throw error;btn.textContent='✓ Request sent';}catch(error){btn.disabled=false;btn.textContent='🤝 Raise merge request';alert(error.message||'Could not send merge request.')}});loadPosts(viewedId);Promise.resolve().then(()=>supabaseClient.rpc('record_profile_view',{p_viewed_id:viewedId})).catch(()=>{});
  }catch(error){root.innerHTML=`<div class="card"><h3>Profile unavailable</h3><p class="muted">${esc(error?.message||'Unable to load this profile.')}</p><a class="btn btn-primary" href="dashboard.html">← Back to matches</a></div>`}finally{clearTimeout(failTimer)}});
async function loadPosts(userId){const box=document.getElementById('profile-posts');if(!box)return;const{data,error}=await supabaseClient.from('profile_posts').select('id,content,image_url,created_at').eq('user_id',userId).order('created_at',{ascending:false}).limit(12);if(error||!data||!data.length){box.innerHTML='<div class="empty-posts"><span>📝</span><p>No public posts yet.</p><small>Roommate updates, room-hunt posts and introductions will appear here.</small></div>';return}box.innerHTML=data.map(p=>`<article class="profile-post"><div class="post-meta"><strong>${esc(new Date(p.created_at).toLocaleDateString('en-IN',{day:'numeric',month:'short',year:'numeric'}))}</strong><span>Public update</span></div><p>${esc(p.content||'')}</p>${p.image_url?`<img src="${esc(p.image_url)}" alt="Roommate post" loading="lazy">`:''}</article>`).join('')}
function formatValue(v){if(typeof v==='number'&&v>=1&&v<=5)return['Very relaxed','Relaxed','Balanced','Tidy','Very tidy'][Math.round(v)-1];return String(v)}function esc(v){const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML}